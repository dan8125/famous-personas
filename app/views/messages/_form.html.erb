<%= simple_form_for [chat, message], html: { class: "chat-input-form d-flex align-items-center" } do |f| %>
  <div class="input-group position-relative w-100">
    <%= f.input :content,
      label: false,
      placeholder: "Type your message...",
      input_html: { class: "form-control chat-input py-3" },
      wrapper_html: { class: 'w-100' }
    %>
    <button class="btn send-btn position-absolute" type="submit">
      ➤
    </button>
  </div>
<% end %>
